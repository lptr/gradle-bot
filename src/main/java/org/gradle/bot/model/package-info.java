package org.gradle.bot.model;

/*
 Classes under this package are generated from JSON by `json2Java` Gradle task. We can't use Kotlin data class because they contain
 @NotNull annotations in bytecode, which might result in deserialization failures.
 */